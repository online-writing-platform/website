{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "rootDir": ".",
        "outDir": "dist-tools",
        "noEmit": true
    },
    "include": ["prisma/**/*.ts", "scripts/**/*.ts"],
    "exclude": ["node_modules", "dist", "dist-tools"]
}
